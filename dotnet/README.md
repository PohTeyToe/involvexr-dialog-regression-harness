# dotnet/

Phase 2 stub. See the root [README](../README.md) for the project plan.

This folder will host a thin C#/ASP.NET wrapper around the Python harness so
that a Lumeto-style backend can shell out to it from existing SpecFlow or
xUnit suites without rewriting assertions twice.
